/* global salesReporterFactory */

'use strict';

const assert = chai.assert;

describe('Test Dummy Form - Costume Shop Sales', function () {

    let salesReporter;

    beforeEach(function () {
        salesReporter = salesReporterFactory.getReporter();
    });

    describe('sales report', function () {
        it('should return a sales report for a single sale', function () {
            
        });
    });

});
